react-workspace

폴더생성 : pre-todo-node-react

	server -> npm init npm i express oracledb cors
		server.js 생성
		server에서 열어주는 포트번호와 리액트에서 보여주는 포트번호는 달라야함
		하지만 리액트에서 axios로 받아올 포트번호와 백엔드 포트번호는 같아야함
		현재는 리액트 주소값을 지정해줄 수 없기 때문에 리액트에서 보여주는 
		포트번호를 피해서 만들어 줘야함

	npx create-react-app client -> App.js에 연결된 값 뿌려주기 




카페
	DB -> create cafe

	node-react-cafe

	1. 마우스로 폴더만들기
		cmd (터미널) 창에서 진행한다면 mkdir server
		npm init
		npm i express oracledb cors
		npm start


	2. npx create-react-app client
		npm i axios
		npm start

	 